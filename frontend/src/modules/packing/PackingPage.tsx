import { Empty } from 'antd'
import SectionCard from '../../shared/components/SectionCard'

export default function PackingPage() {
  return (
    <SectionCard title="Packing">
      <Empty description="Packing isn't built yet." style={{ paddingTop: 48 }} />
    </SectionCard>
  )
}
