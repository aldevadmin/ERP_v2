import { Empty } from 'antd'
import SectionCard from '../../shared/components/SectionCard'

export default function TransactionsPage() {
  return (
    <SectionCard title="Transactions">
      <Empty description="Not built yet." style={{ paddingTop: 48 }} />
    </SectionCard>
  )
}
