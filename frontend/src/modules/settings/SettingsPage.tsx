import { Empty } from 'antd'
import SectionCard from '../../shared/components/SectionCard'

export default function SettingsPage() {
  return (
    <SectionCard title="Settings">
      <Empty description="Settings isn't built yet." style={{ paddingTop: 48 }} />
    </SectionCard>
  )
}
