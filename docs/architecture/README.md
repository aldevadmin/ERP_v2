# Architecture

System-level design docs for the ERP platform (module boundaries, shared apps, integration approach).

- `decisions/` — Architecture Decision Records (ADRs). Any significant, hard-to-reverse choice (auth mechanism, whether to build a given shared module now vs. later, data model conventions, etc.) should be recorded here rather than only discussed in chat or a PR description.
