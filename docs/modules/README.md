# Modules

Each business module gets its own folder here holding its functional/business specification — scope, rules, fields, workflows. This is where module-specific detail belongs, separate from the permanent engineering principles in the root [`CLAUDE.md`](../../CLAUDE.md).

Add a new folder when a new module's spec work starts; don't pre-create folders for modules that aren't being worked on yet.
